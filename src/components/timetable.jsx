import { UnifiedSideBar } from "./util";
import { Flex } from "@chakra-ui/react";

function Timetable() {
    return (
        <Flex>
            <UnifiedSideBar bg="grey" />
        </Flex>
    )
}

export default Timetable